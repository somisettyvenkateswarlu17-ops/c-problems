#include <stdio.h>
int main() {
    //Your Code goes here!
    int a,b;
    printf("enter the numbers",a,b);
    scanf("%d %d",&a,&b);
    if(a>b||b>a ? a:b ){
        printf("%d is smallest");
    }
    return 0;
}