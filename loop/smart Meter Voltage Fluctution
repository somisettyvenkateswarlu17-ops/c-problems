#include <stdio.h>
int main() {
    int n,voltage,min;
    scanf("%d",&n);
    int sum=0,count=0;
    for(int i=0;i<n;i++){
        scanf("%d",&voltage);
       if(i==0){
        min=voltage;
       }
        if(voltage<min){
            min=voltage;
           

        }
       if(voltage<210){
           count++;
       }
        
    }
        printf("Minimum Voltage: %d\n",min);
       printf(" Low Voltage Event: %d",count);
    
    return 0;
}
