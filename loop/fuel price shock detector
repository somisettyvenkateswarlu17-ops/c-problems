#include<stdio.h>
#include<limits.h>
int main() {
    int num,fuel_price;
    scanf("%d",&num);
    int sum=0,count=0;
    int max=INT_MIN;
    for(int i=0;i<num;i++){
        scanf("%d",&fuel_price);
        if(fuel_price>100){
            count++;

        }
        if(max<fuel_price){
            max=fuel_price;
        }
        
    }
    printf("high price days: %d\n",count);
    printf("hightest price: %d",max);
    return 0;
}
