#include <stdio.h>

int main()
{
    int i, n = 7, count = 0;

    for(i = 1; i <= n; i++)
    {
        if(n % i == 0)
        {
            count++;
        }
    }

    if(count == 2)
        printf("Prime Number");
    else
        printf("Not Prime");

    return 0;
}
