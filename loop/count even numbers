#include <stdio.h>
int main() {
    int n,count=0;
    scanf("%d",&n);
    for(int i=0;i<=n;i++){
        if(n%2==0){
            count++;
        }
        n/=10;
        }
        printf("%d",count);
    
    return 0;
}
