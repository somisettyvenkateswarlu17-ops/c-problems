#include <stdio.h>
int main() {
    int salary,absentdays;
    scanf("%d",&salary);
    scanf("%d",&absentdays);
    for(int i=0;i<absentdays;i++){
       salary=salary-100;
       }
     printf("%d",salary);
    return 0;
}
